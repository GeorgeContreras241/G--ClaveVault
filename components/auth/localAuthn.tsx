// this component is for local authentication with master key and archive

export const LocalAuthn = () => {
    return (
        <div>
            <h1>Local Authentication</h1>
        </div>
    )
}